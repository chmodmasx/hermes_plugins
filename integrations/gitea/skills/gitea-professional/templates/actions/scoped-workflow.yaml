name: Organization Policy Check

on:
  push:
  pull_request:

permissions:
  code: read

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@REPLACE_WITH_REVIEWED_COMMIT_SHA

      - name: Policy check
        run: ./ci/policy-check.sh
